#!/usr/bin/env bash
set -euo pipefail
echo "FFmpeg bundle script — download static builds per platform into resources/ffmpeg/"
echo "Full variant CI will populate resources/ffmpeg/{macos-arm64,macos-x64,windows,linux}"
